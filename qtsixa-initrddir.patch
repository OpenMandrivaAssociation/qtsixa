--- qtsixa-1.5.0/sixad/Makefile.initrd	2011-01-26 23:28:21.000000000 -0200
+++ qtsixa-1.5.0/sixad/Makefile	2011-01-26 23:28:36.000000000 -0200
@@ -14,13 +14,13 @@ clean:
 
 install:
 	install -d $(DESTDIR)/etc/default/
-	install -d $(DESTDIR)/etc/init.d/
+	install -d $(DESTDIR)/etc/rc.d/init.d/
 	install -d $(DESTDIR)/usr/bin/
 	install -d $(DESTDIR)/usr/sbin/
 	install -d $(DESTDIR)/lib/udev/rules.d/
 	install -d $(DESTDIR)/var/lib/sixad/
 	install -m 644 sixad.default $(DESTDIR)/etc/default/sixad
-	install -m 655 sixad.init $(DESTDIR)/etc/init.d/sixad
+	install -m 655 sixad.init $(DESTDIR)/etc/rc.d/init.d/sixad
 	install -m 655 sixad $(DESTDIR)/usr/bin/
 	install -m 655 bins/sixad-bin $(DESTDIR)/usr/sbin/
 	install -m 655 bins/sixad-sixaxis $(DESTDIR)/usr/sbin/
@@ -38,7 +38,7 @@ install-system:
 
 uninstall:
 	rm -f $(DESTDIR)/etc/default/sixad
-	rm -f $(DESTDIR)/etc/init.d/sixad
+	rm -f $(DESTDIR)/etc/rc.d/init.d/sixad
 	rm -f $(DESTDIR)/usr/bin/sixad
 	rm -f $(DESTDIR)/usr/sbin/sixad-bin
 	rm -f $(DESTDIR)/usr/sbin/sixad-sixaxis
